# Reconcile — Generated Audit Snapshot

> **Generated:** 2026-09-25, from source at commit `1c94d69`.
> This file is a generated audit snapshot — do not edit it. See `AGENTS.md` § "Generated audit docs". When code described here changes, a stale marker line is prepended above this block; nothing else in the file is ever modified by hand.

**Scope.** The shared freshness machinery in `internal/reconcile/`: how staleness is evaluated across the dependency graph of live `active` projections and reflections (`evaluator.go`), the pending-candidate currency axis a projection's verdict now carries, how a windowed reflection is evaluated per window, and the reconcile wave that drains the stale set (`worker.go`): one wave's run, its per-entity generation rules (including the engaged-candidate skip), every entry point that requests a wave, how an interactive generation cancels a running wave (`internal/workers/manager.go`), and the in-process wave state that `GET /api/reconcile` and `GET /api/status` publish together with the surfaced wave policy flag. This doc was formerly named `rotation.md`; the codebase's vocabulary is now *reconcile* throughout. Entity-agnostic: per-entity generation is in `lifecycle-projection.md` / `lifecycle-reflection.md`, spec resolution in `context.md`, window calculation in `windows.md`. The HTTP surface (`GET /api/reconcile`, `POST /api/reconcile`) is indexed in `api.md` § 9 together with the aggregate status route; the worker's place in the boot order is in `boot-and-workers.md` § 2. What happens when a wave meets another kind of in-flight work — the outcome of each pairing — is `coordination.md`; this doc describes the mechanisms it points at.

**Completeness anchor.** The evaluator is constructed at exactly 3 sites (`NewEvaluator(` in non-test Go: the wave in `worker.go`, `EvaluateEntity` in `evaluator.go`, `HandleGetReconcile` in `handlers/reconcile.go`), and `reconcile.EvaluateEntity` is called at exactly 3 handler sites, all listed in § 1. A wave is requested at exactly 12 sites: 1 call to `StartWave` and 11 sites that request through `EnqueueWave` (10 non-test sites outside `internal/reconcile/` — 9 direct calls plus the method value handed to `refinement.Commit` — plus the worker's own `OnMapSettled`), all listed in § 4. A wave is cancelled at exactly 1 site (`CancelWave`, from `workers.Manager.yieldWave`), reached from the 3 manager generation methods listed in § 3.2. The worker keeps exactly 8 fields under its state lock and publishes 7 wire fields (§ 5).

---

## 1. The evaluation pass

One pass (`reconcile.Evaluator.EvaluateAll`, constructed with a `now`) evaluates **every live `active`** projection and reflection together. Both loads filter `status = 'active' && deleted_at = ''` (`engine.LiveFilter`): a `proposed` entity or a soft-deleted one is not loaded, so it is neither a node nor an edge endpoint, can be neither stale nor blocking, and is absent from the response. A load error aborts the pass with that error.

1. **Graph build.** Each entity is a node carrying its own `current_context_spec` (a spec that fails to decode is treated as empty — an isolated node). Edges come from the spec's `sourceProjectionIds` / `sourceReflectionIds`; a reference to an id that is not a node (non-existent, `proposed`, or soft-deleted) produces no edge. Edges are built from every node's spec regardless of entity type.
2. **Topological order.** A depth-first visit appends each node after its dependencies, so dependencies precede dependents in the result. The visit never fails: on reaching a node already on the visiting stack (a cycle) it returns without error and without visiting further, so cyclic members are emitted in whatever order the traversal reached them. Root iteration is over a Go map, so the relative order of independent nodes is not stable between passes.
3. **Per-node evaluation** (§ 2), in that order, producing one `EntityStatus{id, type, upToDateSnapshotId?, newFragmentIds?, staleDependencies?, blockedBy?, pendingWindows?, staleWindows?, candidate?}` per entity (`api.EntityStatus` is an alias of `api.ReflectionStatus`; `type` is `projection` or `reflection`). An error in any node — spec resolution, the pending-candidate lookup, or the engagement query — aborts the whole pass with that error; nothing partial is returned.

The pass runs on demand and is never cached or scheduled. It is constructed at three sites:

| Site | Caller | On evaluation error |
|---|---|---|
| `handlers.HandleGetReconcile` | `GET /api/reconcile` (`api.md` § 9): the full list is returned as `statuses`, flattened beside the wave state of § 5 | logs `reconcile evaluation failed`; `500 failed to evaluate staleness` |
| `reconcile.EvaluateEntity` | runs the full pass with `time.Now()` and picks the one row whose `id` matches; an id not in the results (not live-active) yields a **zero** status (empty `id` and `type`, nothing stale, no `candidate`). Called from: `HandleGenerateCandidate` (`POST /api/projections/{id}/candidates` and `…/snapshots`), for the `blockedBy` refusal and the engaged-candidate refusal; `HandleGenerateReflectionSnapshot` (`POST /api/reflections/{id}/snapshots` and `…/generate-snapshot`), for the `blockedBy` refusal and the stale-window candidates handed to `reflections.WindowsToGenerate` (`lifecycle-projection.md` § 4, `lifecycle-reflection.md` § 5.1); `HandleListReflectionWindows` (`GET /api/reflections/{id}/windows`), for the per-window `stale` flag (`lifecycle-reflection.md` § 9) | both generate handlers log `staleness check failed` at warn and proceed with the zero status (no `409 upstream…`, no stale-window candidates); the projection handler then re-derives engagement on its own (`engine.FindPendingCandidate` + `engine.CandidateEngaged`, errors of both ignored) so the engaged refusal still applies; the windows listing discards the error without logging (every window `stale: false`) |
| `reconcile.runWaveWithWorker` | the start of every wave (§ 3) | logs `wave evaluate failed`; the wave ends with error `evaluate: …` (§ 5) |

The generate handlers refuse with `409 upstream dependencies are not up to date; approve them first` only when the returned `blockedBy` is non-empty; `newFragmentIds`, `staleDependencies` and the window lists never refuse an interactive generation. The projection handler additionally refuses with `409 cannot replace engaged candidate; discard it first` (error data `{kind: "candidate_engaged"}`) when `candidate.engaged` is true and the bound request's `discardEngaged` is false; the `blockedBy` check runs first, so a blocked entity with an engaged candidate reports the upstream error. Reflections have no candidate axis and no engaged refusal.

## 2. Per-entity verdict

1. **Live snapshot.** The `approved` snapshot with the highest `approval_sequence_number` for the entity (`<fk> = id && status = 'approved'`, ordered `-approval_sequence_number`, limit 1) — for reflections across **all** window keys. A query error counts as none.
2. **Candidate axis (projections only, § 2.2).** Before the draft test, the projection's pending candidate is looked up and, when one exists, its own currency and engagement are reported as `candidate`. This runs for drafts too, so a projection with no approved snapshot but a pending candidate still carries `candidate`; and it resolves the spec, so a draft's spec resolution error now aborts the pass.
3. **Draft.** No live snapshot → the status carries only `id`, `type` and any `candidate` (no `upToDateSnapshotId`, nothing stale). Drafts never block dependents (step 6).
4. **Reflections with a windowed approved snapshot** → § 2.1, and evaluation ends there.
5. **Context diff.** The entity's `current_context_spec` (the entity's, **not** the lens row's) is resolved now, unwindowed, with the caller's context — none of the three callers marks a generation trigger, so upstreams resolve in ordinary approved-only mode (`context.md` § 2–3: live fragments only, upstreams with `deleted_at = ''` only, newest `approved` snapshot per upstream by `approval_sequence_number`). A projection that already resolved its spec for the candidate axis reuses that result rather than resolving twice. The result is diffed **one way** against the live snapshot's `resolved_context` (`PinnedIDs.Diff`, `context.md` § 5; a receipt that fails to decode is treated as empty; `expandedIds` is ignored): `newFragmentIds` = fragments now in scope that the snapshot did not consume, in resolution order, unsorted. Each snapshot id now in scope but not consumed is looked up in `projection_snapshot`, then `reflection_snapshot`, and mapped to its parent entity as a candidate `staleDependencies` entry; an id found in neither is dropped silently.
6. **Blocked.** Each direct upstream (edge) whose own verdict has no `upToDateSnapshotId` **and** that has at least one `approved` snapshot (a fresh query; a query error counts as none) is `blockedBy`. A draft upstream never blocks. In a cycle, the member evaluated first reads its not-yet-evaluated upstream's zero verdict, so that upstream is reported as blocking whenever it has an approved snapshot, whatever its own freshness.
7. **Precedence.** An upstream that is both a stale dependency and blocking is reported only in `blockedBy`. Both lists are sorted.
8. **Reflections with only windowless snapshots** additionally report `pendingWindows` = `reflections.PendingWindows`: every window of the series (§ 2.1) with no approved snapshot and no open generation claim (`windows.md` § 3, § 6). No `staleWindows` are produced on this path.
9. **Up to date** ⇔ no new fragments, no stale dependencies, no blockers, no pending windows; then `upToDateSnapshotId` = the live snapshot's id. The candidate axis plays no part in this test: an entity is up to date or stale on its approved snapshot alone, whatever its candidate reports.

Consequences of the one-way diff and the live filters:

- Fragments in the snapshot's receipt that are no longer in scope — soft-deleted, dropped from a colour, unpinned, or of a type removed from the spec — do **not** make an entity stale. Explicitly pinned `fragmentIds` are a static set: a new fragment makes a pin-only entity stale only when it is added to the spec.
- A soft-deleted upstream is excluded three ways: it is not a node (no edge, cannot block or be a stale dependency), its snapshots are excluded from resolution (`projection_id.deleted_at = ''` / `reflection_id.deleted_at = ''`), and because the diff is one-way its disappearance from scope flags nothing. Restoring it reinstates all three.
- A lens change or a model change does not make an entity stale here: the approved-snapshot verdict reads neither `lens_id` nor `generated_by_model`. Those surface as `candidate.reason` (`lens_changed` / `model_changed`, § 2.2) when a pending candidate exists, as `lensOutdated` in the windows listing (`lifecycle-reflection.md` § 9), as a candidate window in `WindowsToGenerate`, and as non-current in the wave's `SnapshotIsCurrent` guard (§ 3.1).
- A `pending_review` candidate is invisible to the approved-snapshot verdict: only approved rows are compared, and an entity whose only newer output is a candidate stays stale until that candidate is approved. The candidate's own state is reported beside it (§ 2.2), not folded in.

### 2.1 Windowed reflections

The series is `reflections.SeriesWindows` (`windows.md` § 6): the governing grid, every `reflection_window` backfill row, and every window holding an `approved` or `generating` snapshot, sorted by `start` then `end`. The path applies when any series window has an approved snapshot. For each window, oldest first:

- **No approved snapshot**: `pendingWindows` unless a generation claim is open for it (a generating window appears in neither list).
- **Approved snapshot**: that window key's approved snapshot with the highest `approval_sequence_number` is loaded (a query error or an empty result skips the window silently); it becomes the candidate `upToDateSnapshotId` when its window's `end` is the greatest seen so far (string comparison of the RFC3339 bound); the spec is resolved **inside the window** (`context.md` § 2.1); a resolution error skips the window silently. The one-way diff's new fragments mark the window `staleWindows` and join `newFragmentIds` (deduplicated, then sorted). New snapshot ids in the diff are ignored on this path.

No `staleDependencies` or `blockedBy` are computed on this path even when the reflection's spec names upstreams (its edges still exist, and its verdict still decides whether *it* blocks its dependents). Up to date ⇔ no new fragments, no stale windows, no pending windows; `upToDateSnapshotId` = the approved snapshot of the window whose `end` is greatest.

### 2.2 The candidate axis (projections)

For a projection node, `engine.FindPendingCandidate` loads the newest `pending_review` row (`projection_id = id && status = 'pending_review'`, ordered `-created`, limit 1; a query error aborts the pass). None → no `candidate` field. One → `candidate: {id, outdated, reason?, engaged, newFragmentIds?}`:

- **`engaged`** = `engine.CandidateEngaged`: true when any entry of the candidate's `edits` JSON has `type` `manual` or `refinement` (`regeneration` entries — the machine's own diff markers — do not count), or when at least one `projection_refinement` row has `projection_snapshot_id` = the candidate's id. A query error on the refinement lookup aborts the pass. What writes those edits and rows is `lifecycle-projection.md` § 4.4 and `refinement.md` § 2.
- **`outdated` / `reason`** = `engine.SnapshotCurrency(entity, candidate, resolvedNow)` with the spec resolved unwindowed, approved-only, as in § 2 step 5 — the same three checks as the wave's `SnapshotIsCurrent` (§ 3.1 step 4), in this order: the candidate's `lens_id` differs from the entity's `current_lens_id` → `lens_changed`; the candidate's `generated_by_model` is non-empty and `llm.ResolveRoleFor(RoleSnapshot, generate_with_model)` resolves without error to a different model → `model_changed`; the two-way diff of the candidate's `resolved_context` against the resolution has anything added or removed → `context_changed`. Current → `outdated: false`, `reason` omitted.
- **`newFragmentIds`** = the one-way diff of the resolution against the candidate's `resolved_context` (fragments only, unsorted), computed whatever the reason. When the reason is `context_changed` and this list is non-empty, the reason is narrowed to `new_fragments`; a `context_changed` with an empty list (something left scope, or an upstream snapshot changed) stays `context_changed`.

The candidate axis is per-entity, not per-window: the lookup passes a nil window, and the reflection strategy is never consulted, so reflections never carry `candidate`.

## 3. The reconcile wave

A wave is one call of `reconcile.runWaveWithWorker`, executed by the single `reconcile.Worker` goroutine (`Run`) each time its `workerutil.Signal` fires (§ 4). Before the call `Run` derives a cancellable `waveCtx` from the worker's lifetime context and, under the state lock, sets `running = true`, `lastStarted = now`, clears `currentEntity` and `progress`, and stores the cancel function (§ 3.2). The wave:

1. Runs one evaluation pass (§ 1) with `time.Now()` under `waveCtx` — no generation trigger, so upstreams resolve approved-only and the worklist is exactly what `GET /api/reconcile` reports. An evaluation error ends the wave.
2. Builds the generation context: `queue.Background` priority (`llm-queue-quota.md` § 2.1; preemptible by interactive arrivals, § 2.4) and the generation trigger `generate_all` (`llmcontext.WithGenerationTrigger`). The trigger switches upstream resolution from "newest `approved` by `approval_sequence_number`" to "newest by `created` of any status other than `generating` / `discarded`", i.e. a `pending_review` candidate or an approved row (`context.md` § 3), and is stamped on every row the wave writes as `generation_trigger`.
3. Fixes the worklist: every status, in the returned order (dependencies before dependents), for which any of `newFragmentIds`, `staleDependencies`, `blockedBy`, `pendingWindows`, `staleWindows` is non-empty. Drafts and up-to-date entities are excluded; the `candidate` axis alone never puts an entity on the worklist (an outdated candidate over an up-to-date approved snapshot is not regenerated by the wave). `progress` is published as `{completed: 0, total: len(worklist)}` — until this point a running wave reports no `progress`.
4. Walks the worklist. Before each entity, a cancelled `waveCtx` ends the wave with `context.Canceled`. Otherwise `currentEntity = {id, type}` is published and § 3.1 runs. A `blockedBy` entity **is** generated (its blocker was generated earlier in the same walk, and speculative resolution consumes that fresh candidate), unlike an interactive generate, which refuses it with `409`. After each entity, `progress.completed` increments and `currentEntity` clears — an entity that § 3.1 skipped counts as completed.
5. Ends at the first entity error, logged `wave generate failed; ending wave` and returned as `<type> <id>: <err>`; entities later in the order are not visited. Returns nil when every entity that needed work was generated or deliberately skipped.

### 3.1 Generating one entity in a wave

`generateEntity(genCtx, app, status)`:

1. **Strategy and status.** Reflections generate `approved` snapshots (as everywhere else); projections generate `pending_review` review candidates.
2. **Engaged candidate.** A status whose `candidate.engaged` is true (§ 2.2) is logged `wave target skipped; candidate is engaged` at info and skipped before anything is loaded; the entity's pending candidate is left as it is, it counts as completed, and the wave continues. Only the evaluator's verdict is consulted — engagement is not re-checked at generation time, so a candidate engaged after the pass and before its turn is replaced as usual.
3. **Liveness.** `engine.FindLive` failing (the entity was hard-removed or soft-deleted since the pass) logs `wave target skipped` at warn and skips the entity; the wave continues.
4. **Which windows.**
   - `pendingWindows` + `staleWindows` non-empty → exactly those windows, pending first, then stale, in status order.
   - otherwise, a reflection whose governing grid is non-empty (`reflections.CurrentGridWindows` at `time.Now()`) → nothing is generated (a scheduled reflection flagged only by `blockedBy` / `staleDependencies` owes no window and never gets a windowless snapshot);
   - otherwise `engine.SnapshotIsCurrent` true → skipped; else one windowless generation.
5. **`SnapshotIsCurrent`** (the dedup guard; windowed generations do not consult it): the entity's newest `pending_review`-or-`approved` snapshot (ordered `created DESC, approval_sequence_number DESC, rowid DESC`; none or a query error → not current) is passed to `engine.SnapshotCurrency` with the entity's `current_context_spec` (the same spec every generation resolves, `refinement.md` § 6) resolved unwindowed under `genCtx` (speculative upstreams; a resolution error → not current). Current iff its `lens_id` equals `current_lens_id`; its `generated_by_model`, when non-empty, equals `llm.ResolveRoleFor(RoleSnapshot, generate_with_model)` (`models.md` § 5; an empty stored model, or a role resolution that errors, never fails this check); and the resolution is identical to the snapshot's `resolved_context` in **both** directions (`llmcontext.DiffPinnedIDs`: nothing added, nothing removed).
6. **Per window**, `engine.GenerateSnapshot(genCtx, …)` (`lifecycle-projection.md` § 4.1–4.2, `lifecycle-reflection.md` § 5.2). Before each attempt a cancelled `waveCtx` returns `context.Canceled`. Outcome mapping: `queue.ErrPreempted` → the same window is retried at once (the retry blocks in the scheduler until a slot frees); `engine.ErrLensNotReady` or `engine.ErrGenerationInFlight` → logged `wave target skipped` at warn, this window is abandoned, the next window (if any) proceeds; any other error — including `usage.ErrExhausted`, the prompt-size guard's error, a role-resolution error, `prepare context: this context must contain fragments only, but snapshots were provided` (a reflection whose spec names an upstream entity), `<type> <id>: model returned empty output`, and an error wrapping `context.Canceled` from a cancelled model call — is returned and ends the wave.
7. **What a wave generation writes.** `GenerateSnapshot` claims the per-target (per-window) `generating` row; inside its lifetime the spec is resolved speculatively (step 2 of § 3; a resolution error at this point yields an **empty** source block and an empty receipt, not an error). Output is minimised against the newest approved snapshot of the same lens as for any generation; then:
   - **Settle in place.** When the output equals the approved output (byte-for-byte, or the delta turn reports no change) **and** the context carries a generation trigger — or the fold-in mark an interactive `foldIn: true` request sets, `lifecycle-projection.md` § 4 — `settleApprovedInPlace` runs in one transaction: if the newest approved snapshot (per window for reflections) still belongs to the current `lens_id`, its `context_spec`, `resolved_context`, `generated_by_model` and `generated_at` are overwritten, every other `pending_review` sibling is set `discarded`, no new row is written, the claim row is released (deleted) and the approved snapshot's id is returned. Its `approval_sequence_number` does not move, so dependents that consumed it are not made stale. If the approved snapshot has moved (none, or another lens), the generation falls through to the ordinary store; a transaction error is returned as `settle in place: …` and ends the wave. An interactive generation without the fold-in mark that changes nothing still produces a candidate.
   - **Otherwise** the claim row is filled in place with `generation_trigger = generate_all`, `output_draft` and `edits` carrying the block diff against the approved output, other pending siblings discarded, and waiters on the claim are woken; a reflection's row is then approved (`ApproveSnapshot`).
8. **Downstream consequences.** Approval promotes a candidate in place (same id, `lifecycle-projection.md` § 4.3), so a dependent whose wave candidate consumed that row reads as consistent without regeneration. A refinement committed on a still-`pending_review` **projection** candidate approves that same row in place (`engine.CommitRefinement`), so the row keeps whatever `generation_trigger` it was written with; a projection commit with no pending source appends a fresh row with an empty trigger (`InheritsCandidateTrigger` is true for projections and false for reflections, but the branch that would stamp the inherited trigger on an appended row is only reached when no pending source exists, where the inherited value is empty); every commit, chain or not, requests a wave (§ 4). An interactive generate that finds the wave's claim row open joins it (`engine.GenerateOrJoin` → `AwaitGeneration`, `lifecycle-projection.md` § 4.1) rather than failing; when that wave generation settles in place its claim row is deleted unfilled, the joiner reads `ErrGenerationAbandoned` and generates once more from scratch, without the trigger.

### 3.2 Cancelling a wave (yield)

`Worker.CancelWave()` takes the stored cancel function under the state lock and clears it; it reports `true` — and stamps `lastCancelled = now` — only when a cancel function was stored **and** `running` was true; it then calls the function whenever one was stored. The cancel reaches `waveCtx` and therefore `genCtx`: a model call waiting for admission in the scheduler returns the context error, a call in flight is aborted and returns `stream interrupted: context canceled`, `GenerateSnapshot` releases its claim row, and the wave ends with that entity's error (§ 3 step 5) — or with `context.Canceled` at the next per-entity / per-window check. A generation past its last model call is not interrupted: it stores its row and the wave ends at the next check. Entities the wave had not reached are left as they were; nothing re-requests them except the entry points of § 4.

The only caller is `workers.Manager.yieldWave(strat, id)`, run at the start of each of the manager's three interactive generation methods — `GenerateProjectionSnapshot` (from `HandleGenerateCandidate`), `GenerateReflectionSnapshot` (from `HandleGenerateReflectionSnapshot` when exactly one window is to be generated) and `GenerateReflectionWindows` (the same handler, several windows). `yieldWave` first asks `engine.HasLiveClaim(strat, id)`: a `generating` row for that entity (any window) younger than the 10-minute claim TTL means the wave (or another request) is already producing it, so nothing is cancelled and the caller joins that run; otherwise — including when the claim query errors — `CancelWave()` is called whether or not a wave is running, and its result is remembered as `yielded`. After the generation, `afterGenerate(status, produced, yielded)` calls `EnqueueWave()` when `yielded` is true **or** when the generation produced a snapshot with status `approved` (`GenerateReflectionWindows` counts any window succeeding as produced). That request is subject to the automatic-wave policy (§ 4): with `autoWave` off, a wave cut short by an interactive generation is not resumed until the next `POST /api/reconcile`.

## 4. Wave entry points

The worker is built by `workers.New` (`boot-and-workers.md` § 1) with `reconcile.Options{AutoWave}`; its `Run` loop is started when the app serves, then `Manager.BootKicks` runs `LogPolicy` (the boot line `automatic waves on` / `automatic waves off; KALAIDO_AUTO_WAVE=1 enables them; the dashboard starts the wave`) and calls `EnqueueWave` once. The worker wakes on a `workerutil.Signal`, a channel of capacity 1: a notification that finds one buffered is dropped, so any number of requests during a running wave coalesce into exactly one follow-up wave (sound because each wave re-derives its worklist). At shutdown the worker's context is cancelled: a wave in progress ends as in § 3.2 (its outcome recorded as cancelled, not as an error), `Run` returns the context error, and both timers below are stopped and the retry count zeroed.

Two triggers exist:

- **`StartWave()`** — the explicit trigger: stops and clears any pending debounce timer, then signals at once. It works whether or not automatic waves are enabled.
- **`EnqueueWave()`** — the automatic trigger: a no-op unless `autoWave` is set; otherwise arms a 3 s debounce timer (`Options.Debounce`; zero means 3 s) or resets the one already armed, and its expiry signals. A burst of triggers within the quiet period yields one wave. The timer pointer is kept after it fires, so a later request resets the fired timer rather than creating one; the effect is the same.

`autoWave` comes from `KALAIDO_AUTO_WAVE`, parsed once at boot by `config.LoadEnv` (`parseBool`): after trimming and lower-casing, unset, `0`, `false`, `no` and `off` are **off**; `1`, `true`, `yes` and `on` are **on**; any other value fails `LoadEnv` with `KALAIDO_AUTO_WAVE: unparseable boolean value "…"`, which `cmd/sidecar/main.go` prints to stderr before exiting `1`. `main.go` passes it as `server.Options{AutoWave}` → `workers.Options` → `reconcile.Options`; `server.New` / `NewWithSchema` pass the zero `Options`, so a server built through them never starts a wave on its own. `WaveEnabled()` returns it and is published as `policy.wave` by `GET /api/status` (§ 5). With it off, the boot request, every hook and every handler-side request below are silent and the only waves are those `POST /api/reconcile` starts and the retries of § 5.

| # | Site | Trigger | When |
|---|---|---|---|
| 1 | `handlers.HandleReconcile` | `StartWave` | `POST /api/reconcile` → `202` no body, always (`api.md` § 9); a nil worker is tolerated and still answers `202` |
| 2 | `workers.Manager.BootKicks` | `EnqueueWave` | once per serve start, after the worker goroutines are started and after `LogPolicy` (`boot-and-workers.md` § 1) |
| 3 | `refinement.Commit`, given `deps.Reconcile.EnqueueWave` by `handlers.handleCommitRefinementGeneric` | `EnqueueWave` | after **every** refinement commit whose `engine.CommitRefinement` transaction succeeded, projection or reflection, chain-marked or not, before a reflection's pending-window pass is started (`refinement.md` § 5) |
| 4 | `maps.OnSettle(rec.OnMapSettled)` in `workers.New` | `EnqueueWave` | after every map cycle, registered second after `colour.OnMapSettled` so thing-backed membership is recomputed first (`map.md` § 3.2, `colours.md` § 3) |
| 5 | `col.OnDrained(rec.EnqueueWave)` in `workers.New` | `EnqueueWave` | after a colour-worker drain that wrote at least one membership link (`colours.md` § 4.1) |
| 6 | `server.RegisterTriggers` fragment `OnRecordAfterCreateSuccess` | `EnqueueWave` | every fragment created with `ingested_via != "import"` (`ingestion.md` § 7) |
| 7 | `ingest.processIngestRecord` | `EnqueueWave` | when an async batch finishes with at least one fragment written, whether the batch ended `done` or `error`, before the post-import handoff (`ingestion.md` § 3) |
| 8 | `handlers.HandleCreateColour` | `EnqueueWave` | every successful colour create, after seeding (`colours.md` § 5) |
| 9 | `handlers.HandleUpdateColour` | `EnqueueWave` | only when the `prompt` changed, after `Rematch` succeeded; name/example-only updates do not request one |
| 10 | `handlers.HandleRematchColour` | `EnqueueWave` | every `POST /api/colours/{id}/rematch` whose `Rematch` succeeded |
| 11 | `handlers.HandleApproveCandidate` | `EnqueueWave` | after every successful `POST /api/projections/{id}/candidates/{rid}/approve` (reflections have no approve route) |
| 12 | `workers.Manager.afterGenerate` | `EnqueueWave` | after every manager-run interactive generation that cut a wave short, or that produced an `approved` snapshot (§ 3.2) |

Nothing else requests a wave: colour delete, fragment delete, entity delete/restore, schedule edits, the backfill route and the pending-window pass (`lifecycle-reflection.md` § 5.3), discover runs, hand edits and edit-status changes on a candidate, the fold-in carry of a refinement, and config changes do not. Retry waves (§ 5) reuse the signal directly and are not subject to `autoWave` or the debounce.

## 5. Published wave status and retry

The worker keeps eight in-process values under one lock: `running`, `lastStarted`, `lastError`, `lastCompleted`, `lastCancelled`, `currentEntity`, `progress`, `waveCancel`. They are not persisted; a restart zeroes them.

- On each signal, before the wave: `running = true`, `lastStarted = now`, `currentEntity = nil`, `progress = nil`, `waveCancel` set — together, so a reader never observes a new `lastStarted` without `running`. `progress` appears once the worklist is fixed; `currentEntity` is set and cleared per entity (§ 3).
- After the wave (`afterWave`): `running = false`, `currentEntity = nil`; `progress` is left as the wave last set it. A nil result sets `lastError = ""` and `lastCompleted = now`, resets the retry count, and cancels any pending retry timer. An error wrapping `context.Canceled` (a cancel of § 3.2 or shutdown) sets `lastError = ""` and touches nothing else — `lastCompleted` and the retry count keep their values. Any other error sets `lastError = err.Error()` and leaves `lastCompleted` as it was.
- **Retry.** A failed wave whose error wraps `usage.ErrExhausted` (`llm-queue-quota.md` § 5) is logged `wave quota exhausted; not retrying`; the next real trigger re-enters. A cancelled wave is not retried. Any other failure arms a retry timer at `defaultRetryBackoff[min(retries, 2)]` — 1 min, then 5 min, then 15 min for every further consecutive failure — replacing any pending retry timer, logs `wave retrying` with the delay, and the timer signals the worker directly (no debounce, no `autoWave` check — a failed manual wave retries on its own). The count resets only on a clean wave.

`Worker.Status()` returns a consistent copy (`State{Running, LastStarted, LastError, LastCompleted, LastCancelled, CurrentEntity, Progress}`, the two pointers deep-copied). `Worker.EvaluateStatus()` maps it to the wire type `api.ReconcileStatus`: `running`; `lastStarted`, `lastCompleted`, `lastCancelled` as RFC3339 UTC, omitted while zero; `lastError` omitted while empty; `currentEntity: {id, type}` and `progress: {completed, total}` omitted while nil. Two routes publish it: `GET /api/reconcile` embeds it at the top level of `api.ReconcilePlanResponse` beside `statuses` (a nil worker yields `running: false` and nothing else), and `GET /api/status` (`status.Evaluate`) carries it as `reconcile` alongside `policy.wave` = `WaveEnabled()`, both omitted from the computation when no worker is wired. Nothing in the backend reads the status back; how a client pairs `lastStarted` with `running` to detect that a wave it started has ended is client behaviour and out of scope.
